# ⚡ FastAPI Project Generator

`create_fastapi_project.py` is a lightweight, zero-dependency CLI tool that generates a clean and scalable FastAPI project structure.

It is designed for developers who want an easy way to bootstrap new FastAPI applications without installing heavy frameworks or dealing with Click version conflicts.

---

## 🚀 Features

- Generate a complete FastAPI project in seconds
- No external dependencies (pure Python)
- Clean and extendable folder structure
- API versioning (v1)
- Preconfigured `main.py`
- Includes starter `requirements.txt` and `README.md`

---

## 📦 Installation

You can simply drop the script into any folder and run it.

Make it executable:

```bash
chmod +x create_fastapi_project.py
